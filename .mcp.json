{
  "mcpServers": {
    "firebase": {
      "command": "firebase",
      "args": ["experimental:mcp"]
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium"]
    }
  }
}
